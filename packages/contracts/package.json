{
  "name": "@memecho/contracts",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "python ../../services/gateway/scripts/generate_types.py",
    "check:generated": "python ../../services/gateway/scripts/generate_types.py --check",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
